{
  "name": "coach-app-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for CoachApp",
  "scripts": {
    "postinstall": "npm run install:all",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "concurrently \"npm start --prefix frontend\" \"npm start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
